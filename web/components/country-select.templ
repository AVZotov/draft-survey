package components

import "github.com/AVZotov/draft-survey/internal/types"
import "github.com/AVZotov/draft-survey/internal/handler/chi/fields"

templ CountrySelect(countries []types.Country, selected string) {
	<div class="select-wrap">
		<select
			name={ fields.FieldCountryCode }
			id={ fields.FieldCountryCode }
		>
			<option value="">Select country…</option>
			for _, c := range countries {
				<option
					value={ c.CountryCode }
					selected?={ c.CountryCode == selected }
				>
					{ c.Name }
				</option>
			}
		</select>
	</div>
}
