package tanks

import (
	"fmt"
	"github.com/AVZotov/draft-survey/data/dictionaries"
	"github.com/AVZotov/draft-survey/internal/constants"
)

templ AddRowForm(surveyID string, draftIndex int, isOOB bool) {
	<div
		id="add-bw-row"
		class="add-row"
		if isOOB {
			hx-swap-oob="outerHTML"
		}
	>
		<div class="add-row-fields">
			<div class="add-field">
				<label class="add-field-label">Type</label>
				<div class="add-select-wrap">
					<select class="add-sel" name={ constants.TankType } id="new-tank-type">
						for _, bwtType := range dictionaries.BallastWaterTankTypes {
							<option value={ bwtType }>{ bwtType }</option>
						}
					</select>
					<svg class="add-sel-arrow" viewBox="0 0 24 24"><path d="M6 9l6 6 6-6"></path></svg>
				</div>
			</div>
			<div class="add-row-sep"></div>
			<div class="add-field add-field--name">
				<label class="add-field-label">Name</label>
				<input
					id="new-bw-tank-name"
					class="add-inp"
					type="text"
					name={ constants.TankName }
					placeholder="e.g. 1S, 2P, Centre…"
					autocomplete="off"
					oninput="document.getElementById('add-bw-row-btn').disabled = this.value.trim() === ''"
				/>
			</div>
		</div>
		<button
			class="add-btn"
			id="add-bw-row-btn"
			type="button"
			disabled
			hx-post={ fmt.Sprintf("/api/v1/survey/%s/tanks/%d/bw-tank", surveyID, draftIndex) }
			hx-target="#bw-tanks-items"
			hx-include="closest .add-row"
			hx-swap="beforeend"
		>
			<svg viewBox="0 0 24 24"><path d="M12 5v14M5 12h14"></path></svg>
			Add BW tank
		</button>
	</div>
}
