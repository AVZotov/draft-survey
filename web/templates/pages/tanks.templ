package pages

import "github.com/AVZotov/draft-survey/web/components"
import "github.com/AVZotov/draft-survey/web/layouts"
import "github.com/AVZotov/draft-survey/web/widgets"
import "github.com/AVZotov/draft-survey/web/widgets/tanks"

templ Tanks(props components.LayoutProps, tanksProps components.TanksPageProps) {
	@layouts.Layout(props) {
		@widgets.HeaderUser(props.User)
		@tanks.Form(props, tanksProps)
		@tanks.TanksActionBar(tanksProps)
		@widgets.Footer()
	}
}
